{
  "name": "api",
  "sourceRoot": "apps/api/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "pnpm -F api serve" }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "pnpm -F api build" }
    },
    "prepare": {
      "executor": "nx:run-commands",
      "options": { "command": "pnpm -F api prepare" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'lint placeholder'"
      }
    }
  }
}
